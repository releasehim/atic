# Examen de Promoción - FOD - Julio 2025
En cada pregunta hay que marcar una y solo una respuesta. Bien respondida suma un punto, mal respondida resta 0,5, Sin responder es neutra. El alumno debe marcar con un círculo bien claro el inciso correspondiente a su respuesta.

1. ¿Cuál de las siguientes afirmaciones sobre archivos secuenciales, secuenciales ordenados y directos es correcta?
   a. En un archivo serie ordenado, las inserciones se realizan más rápido que en un archivo directo.
   b. Los archivos secuenciales permiten acceso directo si los registros están numerados consecutivamente.
   c. Los archivos directos permiten acceso aleatorio a los registros mediante una función hash, mientras que los secuenciales requieren lectura secuencial.
   d. Un archivo serie ordenado nunca requiere reorganización, ya que siempre mantiene el orden automáticamente.
   e. Algunas de las anteriores son correctas.
   f. Ninguna es correcta.

2. Un archivo con registros de longitud variable
   a. Admite bajas lógicas
   b. Admite bajas físicas
   c. Admite política de mejor ajuste para aprovechamiento de espacio
   d. Todas las anteriores son válidas
   e. Algunas de las anteriores son válidas
   f. Ninguna de las anteriores

3. Dado un archivo, que contiene datos de un problema específico:
   a. Es conveniente que sea de registros de longitud variable
   b. Es conveniente que sea de registros de longitud fija
   c. Puede tener registros de longitud fija y variable al mismo tiempo
   d. Puede tener registros de longitud fija con campos de longitud variable
   e. Puede tener registros de longitud fija con campos de longitud fija
   f. Algunas de las anteriores son validas

4. En el contexto de la gestión de archivos, considere las siguientes afirmaciones sobre el borrado lógico y el borrado físico:
   a. El borrado lógico implica marcar el registro como eliminado sin modificar la estructura física del archivo.
   b. El borrado físico libera el espacio ocupado por el registro y puede requerir reorganización del archivo.
   c. El borrado lógico permite la recuperación del registro sin restaurar una copia de seguridad.
   d. El borrado físico suele ser más rápido que el lógico en archivos secuenciales grandes.
   e. Todas las anteriores son correctas
   f. Algunas de las anteriores es correcta
   g. Ninguna de las anteriores es corrrecta

5. Sea un algoritmo que actualización maestro con archivos detalles:
   a. Se puede implementar si los archivos (maestro y detalles) están ordenados por el mismo criterio
   b. Se puede implementar con archivo maestro esta ordenado y los detalles desordenados
   c. Se puede implementar los archivos maestro y detalles ordenados por criterios diferentes
   d. Se puede implementar si los archivos (maestro y detalles) están desordenados
   e. Todas las anteriores
   f. Algunas de las anteriores
   g. Ninguna de las anteriores

6. Un índice secundario es
   a. Una estructura de datos adicional que permite asociar varias claves primarias a una clave secundaria
   b. Una estructura de datos adicional que contiene mayor volumen de información que el archivo original
   c. Una estructura de datos adicional que ordena el archivo original.
   d. Una estructura de datos adicional que permite relacionar una clave secundaria con un registro del archivo original
   e. Todas las anteriores son válidas.
   f. La respuesta a y la respuesta c son correctas
   g. La respuesta a y la respuesta d son correctas.

7. Dado un árbol B+ de prefijos simples. El mismo genera la estructura para un índice de un archivo. En ese caso:
   a. Para buscar un elemento en esa estructura siempre se llega a nivel hoja
   b. Los nodos hojas pueden estar enlazados entre si para permitir un acceso secuencial a los datos a bajo costo.
   c. Los nodos internos conforman un índice para llegar al elemento buscado y contiene copias de las claves que están en nodos hoja
   d. Todas las anteriores son validas
   e. Algunas de las anteriores son validas
   f. Ninguna es valida

8. Se dispone de una estructura que organiza un índice de un archivo. Dicha estructura esta implementada como un árbol B+. Ante una baja de un elemento
   a. Siempre se aplica la redistribución para que el nodo cumpla las propiedades del árbol.
   b. Siempre se aplica fusión o concatenación para que el nodo cumpla las propiedades del árbol.
   c. Siempre se accede a nivel hoja para borrar el elemento
   d. Todas las anteriores son incorrectas.
   e. Algunas de las anteriores s son valida

9. Cuál de las siguientes características corresponde a un árbol binario de prefijos simples?
   h. Cada nodo tiene un elemento y dos hijos, menos los nodos terminales.
   i. El árbol puede desbalancearse fácilmente.
   j. Si el árbol se encuentra balanceado el orden de búsqueda de un elemento es logarítmico en base 2
   k. Todas las opciones anteriores son correctas.
   l. Algunas de las anteriores son válidas.
   m. Ninguna de las opciones anteriores es correcta.

10. En un árbol B+ que utiliza prefijos simples para optimizar el almacenamiento en los nodos internos, ¿cuáles de las siguientes afirmaciones son verdaderas?
    a. Los nodos internos pueden almacenar solo el prefijo común necesario para dirigir la búsqueda.
    b. Los nodos hojas siempre deben contener claves completas y datos asociados.
    c. El uso de prefijos simples reduce el tamaño de las claves de los nodos internos, a lo estrictamente necesario.
    d. Los prefijos almacenados en los nodos internos afectan el orden lógico de las claves en los nodos hoja.
    e. Todas las afirmaciones
    f. Algunas de las afirmaciones
    g. Ninguna de las afirmaciones

11. A medida que la densidad de empaquetamiento aumenta
    a. Se debe cambiar la técnica de resolución de colisiones con desbordes
    b. Se puede cambiar la función de dispersión
    c. Aumenta la probabilidad de colisiones
    d. Aumenta la probabilidad de overflow
    e. Aumenta la capacidad de cada dirección
    f. Todas las respuestas son validas
    g. Las respuestas a, b, c y d son correctas
    h. Las respuestas b, c y d son correctas
    i. Las respuestas c y d son correctas

12. ¿Cuál es la principal ventaja del uso de técnicas de hashing para el acceso a registros en un archivo de datos?
    a. Permiten comprimir el tamaño del archivo.
    b. Mejoran la seguridad del almacenamiento frente a accesos no autorizados.
    c. Facilitan el acceso directo a los registros mediante una función.
    d. Aseguran que los datos se mantengan ordenados alfabéticamente.
    e. Algunas de las anteriores son válidas.
    f. Ninguna de las anteriores

13. ¿Qué ocurre cuando una función de hashing asigna múltiples registros a la misma ubicación en un archivo de datos?
    a. Los registros se descartan para evitar inconsistencias.
    b. Se reordena todo el archivo para evitar colisiones.
    c. Se produce una colisión y puede utilizarse un área de overflow para almacenar los registros adicionales.
    d. La función de hashing se reemplaza automáticamente por otra más eficiente.
    e. Todos los anteriores son válidos
    f. Algunas de las anteriores son válidas.

14. El método de tratamiento de desborde de doble paginación
    a. Es un método más eficiente que el de saturación progresiva encadenada
    b. Es un método que evita la generación de zonas contiguas de overflow
    c. Es un método que no utiliza una zona de overflow separada como el área de desborde
    d. Todas las anteriores son validas
    e. Algunas de las anteriores son válidas
    f. Ninguna de las anteriores son válidas

15. ¿Cuál de las siguientes afirmaciones describe mejor el funcionamiento del hashing extensible en sistemas de archivos?
    a. Utiliza una función hash que cambia automáticamente cuando se produce un overflow en un bloque.
    b. Duplica el archivo completo cada vez que se agrega un nuevo registro para evitar colisiones.
    c. Mantiene una tabla de directorio que puede crecer dinámicamente según la cantidad de registros, permitiendo dividir bloques y evitar rehacer todo el archivo.
    d. Reasigna todos los registros existentes a nuevos bloques cada vez que ocurre una colisión.
    e. Algunas de las anteriores son válidas