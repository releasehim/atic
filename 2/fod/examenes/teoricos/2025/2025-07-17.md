# Examen Teórico - FOD - 17-07-2025 – Nombre y Apellido...........................................................................Legajo:.......................
Las respuestas correctas suman un punto, las incorrectas restan 0,5. Sin responder es neutro. Cada ejercicio tiene un y solo un inciso correcto.

1. Un archivo
   a. Tiene acceso Directo y orden de búsqueda lineal
   b. Tiene acceso Directo y orden de búsqueda logarítmico
   c. Tiene acceso Directo y orden de búsqueda exponencial
   d. Tiene acceso secuencial indizado y orden de búsqueda lineal
   e. Todas las anteriores
   f. Algunas de las anteriores
   g. Ninguna de las anteriores

2. Un proceso de corte de control, para que sea lo más eficiente posible en términos de rendimiento/performance
   a. Se debe aplicar con todos los archivos ordenados por algún criterio
   b. Se debe aplicar con todos los archivos desordenados
   c. Se debe aplicar con algunos de los archivos ordenados por algún criterio y el resto desordenados
   d. Se debe aplicar con cada uno de los n archivos ordenados por diferentes criterios.
   e. Todas las anteriores
   f. Algunas de las anteriores
   g. Ninguna de las anteriores

3. Una clave secundaria
   a. Identifica unívocamente un elemento del archivo
   b. Identifica varios elementos de un archivo
   c. Está constituida por al menos dos atributos del archivo donde está definida
   d. Todas las anteriores
   e. Algunas de las anteriores
   f. Ninguna de las anteriores

4. Un índice implementado con un árbol B
   a. Puede desbalancearse
   b. Puede estar balanceado
   c. Puede balancearse
   d. Se desbalancea fácilmente
   e. Está balanceado
   f. Todas las anteriores
   g. Algunas de las anteriores
   h. Ninguna de las anteriores

5. Dado un índice de un archivo con registros de longitud variable
   a. Sus registros pueden ser de longitud variable
   b. Sus registros deben ser de longitud variable
   c. Sus registros pueden ser de longitud fija
   d. Sus registros son de longitud fija

6. Una función de hash uniforme
   a. Tiene un promedio 1, de acceso a registros
   b. Tiene un promedio entre 1 y 2, de acceso a registros
   c. Tiene un promedio menor a 1, de acceso a registros
   d. Ninguna de las anteriores

7. La densidad de empaquetamiento
   a. Se modifica con cada alta o baja
   b. Se modifica con cada alta
   c. Se modifica con cada baja
   d. Todas las anteriores
   e. Algunas de las anteriores
   f. Ninguna de las anteriores

8. Un índice primario es:
   a. Una estructura de datos adicional que permite asociar una o varias claves primarias con una clave secundaria.
   b. Una estructura de datos adicional que contiene el mismo volumen de información que el archivo original.
   c. Una estructura de datos adicional que permite asociar una o varias claves secundarias con una clave primaria.
   d. Una estructura de datos adicional que establece un orden lógico del archivo original.
   e. Una estructura de datos adicional que permite relacionar una clave primaria con otra clave candidata.
   f. Hay más de una opción válida.
   g. Todas las opciones anteriores son correctas.
   h. Ninguna de las opciones anteriores es correcta.

9. Cuando se realiza una baja en un árbol B:
   a. El elemento que se elimina debe estar en un nodo interno del árbol.
   b. Puede existir underflow y propagarse hasta el nodo raíz.
   c. Siempre es necesario liberar un nodo.
   d. El árbol disminuye de nivel.
   e. No es necesario acceder al nivel hoja.
   f. Hay más de una opción válida.
   g. Todas las opciones anteriores son correctas.
   h. Ninguna de las opciones anteriores es correcta.

10. El método de saturación progresiva encadenada
    a. Reubica las claves en saturación
    b. Reubica las claves en saturación en la cubeta más cercana disponible
    c. Reubica las claves en saturación en la cubeta más cercana disponible generando un enlace con la claves sinónimo
    d. Reubica las claves en colisión que están saturación
    e. Todas las anteriores
    f. Alguna de las anteriores
    g. Ninguna de las anteriores

11. La técnica de hash extensible
    a. No admite colisiones
    b. Admite colisiones, pero no saturaciones
    c. Admite colisiones y saturaciones
    d. Admite colisiones sólo con cubetas con espacio para 2 claves
    e. Todas las anteriores
    f. Algunas de las anteriores
    g. Ninguna de las anteriores

12. Indique el parámetro más importante al aplicar hashing
    a. Función de hashing
    b. Densidad de Empaquetamiento
    c. Método de tratamiento de claves en saturación
    d. Tamaño de los nodos o cubetas
    e. Todos los anteriores
    f. Algunos de los anteriores
    g. Ninguno de los anteriores

13. Para realizar un algoritmo de merge de 100 archivos
    a. Cada archivo debe tener al menos un registro
    b. Cada archivo debe tener al menos dos registros
    c. Todos los archivos deben tener la misma cantidad de registros
    d. Todos los archivos deben tener diferente cantidad de registros
    e. Todas las anteriores
    f. Algunas de las anteriores
    g. Ninguna de las anteriores

14. Si se realiza la baja de un registro en un archivo
    a. El archivo queda lógicamente más pequeño pero físicamente no cambia
    b. El archivo queda lógicamente igual pero físicamente no cambia
    c. El archivo queda lógicamente más pequeño y físicamente cambia
    d. El archivo queda lógicamente igual y físicamente cambia
    e. Todas las anteriores
    f. Algunas de las anteriores
    g. Ninguna de las anteriores

15. Un árbol B+
    a. Es un archivo ordenado
    b. Es un índice ordenado
    c. Es un índice de un archivo de datos ordenado
    d. Es un índice de un archivo de datos desordenado
    e. Todas las anteriores
    f. Algunas de las anteriores
    g. Ninguna de las anteriores